const server = require('../src/server');
const supertest = require('supertest');
const request = supertest(server.app);

describe('Validator Testing: ', ()=> {
    test('blah blah: ', async () => {
        // const response = await 
    })
})